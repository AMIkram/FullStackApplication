package com.example.demo.enums;


// enum : special type of class used to define collections of constants. They are particularly useful when you have a fixed set of values that a variable can take.
public enum AccountStatus {
    CREATED, ACTIVATED, SUSPENDED
}
